import React from 'react';

const Note = ({ onRemove }) => {
    return ( 
        <>
            {/* <button className="delete" onClick={() => onRemove(id)}>X</button> */}
        </>
     );
}
 
export default Note;
